type Props = {};

const SearchField = (props: Props) => {
    return (
        <input
            className=" grow rounded-lg text-xl p-1"
            placeholder="Search"
        ></input>
    );
};

export default SearchField;
